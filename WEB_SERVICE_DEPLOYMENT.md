# Развертывание Frontend как Web Service на Render

## 🚀 Настройки для Render Dashboard:

### 1. Создание Web Service:
- **Type:** Web Service (не Static Site!)
- **Repository:** eco_tracker_ui
- **Root Directory:** ecotracker_ui

### 2. Build & Deploy Settings:
- **Build Command:** `chmod +x build.sh && ./build.sh`
- **Start Command:** `npm start`

**Альтернативно, можно использовать прямые команды:**
- **Build Command:** `npm install && npx vite build`
- **Start Command:** `npm start`

### 3. Environment Variables:
```
VITE_API_URL = https://eco-tracker-server.onrender.com/api
NODE_VERSION = 20.11.0
NODE_ENV = production
```

### 4. Advanced Settings:
- **Instance Type:** Free (или Starter для лучшей производительности)
- **Auto-Deploy:** Yes

## ✅ Преимущества Web Service:

1. **Идеальный SPA роутинг** - все маршруты работают корректно
2. **Сжатие файлов** - быстрая загрузка
3. **Кэширование** - оптимизированная производительность
4. **Полный контроль** - можно добавить middleware
5. **HTTPS** - автоматически включен

## 🔧 Что происходит:

1. **Build:** Собирает React приложение в папку `dist`
2. **Start:** Запускает Express сервер, который:
   - Обслуживает статические файлы из `dist`
   - Обрабатывает SPA роутинг (все запросы → `index.html`)
   - Сжимает файлы для быстрой загрузки
   - Кэширует статические ресурсы

## 📝 После развертывания:

1. Скопируйте URL нового сервиса (например: `https://ecotracker-ui.onrender.com`)
2. Обновите **CORS_ORIGINS** в настройках backend:
   ```
   CORS_ORIGINS = https://ecotracker-ui.onrender.com
   ```
3. Готово! 🎉
