# EcoTracker UI

Клиентская часть системы мониторинга контейнеров EcoTracker.

## Технологии

- **React** + **TypeScript** - UI фреймворк
- **Vite** - сборщик и dev сервер
- **Tailwind CSS** - стилизация
- **shadcn/ui** - компоненты UI
- **Socket.IO Client** - WebSocket для real-time обновлений
- **React Router** - роутинг

## Установка и запуск

### 1. Установка зависимостей

```bash
cd ecotracker_ui
npm install
```

### 2. Настройка подключения к серверу

Скопируйте `.env.example` в `.env.local`:
```bash
cp .env.example .env.local
```

Отредактируйте `.env.local`:

**Для подключения к Render серверу:**
```env
VITE_API_URL=https://eco-tracker-server.onrender.com/api
```

**Для подключения к локальному серверу:**
```env
VITE_API_URL=http://localhost:5000/api
```

### 3. Запуск development сервера

```bash
npm run dev
```

Приложение запустится на `http://localhost:5173`

### 4. Сборка для продакшена

```bash
npm run build
```

Готовые файлы будут в папке `dist/`

## Структура проекта

```
ecotracker_ui/
├── src/
│   ├── components/          # React компоненты
│   │   ├── ui/             # shadcn/ui компоненты
│   │   ├── Layout.tsx      # Основной layout
│   │   └── ...             # Диалоги и формы
│   ├── pages/              # Страницы приложения
│   │   ├── LoginPage.tsx   # Вход
│   │   ├── HomePage.tsx    # Главная
│   │   ├── MonitoringPage.tsx    # Мониторинг
│   │   ├── NotificationsPage.tsx # Уведомления
│   │   ├── LocationsPage.tsx     # Площадки
│   │   ├── ReportsPage.tsx       # Отчеты
│   │   └── AdminPage.tsx         # Администрирование
│   ├── services/           # API клиенты
│   │   ├── api.ts         # HTTP клиент
│   │   ├── authService.ts # Аутентификация
│   │   ├── dataService.ts # Работа с данными
│   │   └── socketService.ts # WebSocket клиент
│   ├── contexts/          # React контексты
│   │   └── AuthContext.tsx # Контекст аутентификации
│   ├── hooks/             # Custom hooks
│   ├── types/             # TypeScript типы
│   └── lib/               # Утилиты
├── public/                # Статические файлы
├── .env.example           # Пример конфигурации
├── .env.local            # Локальная конфигурация (не коммитится)
└── package.json          # Зависимости
```

## Основные страницы

### 🏠 Главная (`/`)
- Обзор статистики компании
- Карты площадок

### 📊 Мониторинг (`/monitoring`)
- Real-time мониторинг всех контейнеров
- Фильтрация по статусам
- Live обновления через WebSocket

### 🔔 Уведомления (`/notifications`)
- Площадки требующие внимания
- Контейнеры со статусом "full"

### 📍 Площадки (`/locations`)
- Управление площадками
- Добавление/редактирование/удаление
- Просмотр контейнеров

### 📈 Отчеты (`/reports`)
- Аналитика и отчеты
- Графики заполнения

### ⚙️ Администрирование (`/admin`)
- Управление пользователями
- Управление компаниями
- Настройки безопасности

## Аутентификация

Приложение использует JWT токены для аутентификации:

1. Пользователь вводит email и пароль
2. Получает access_token
3. Токен сохраняется в localStorage
4. Все запросы к API включают заголовок `Authorization: Bearer <token>`

### Тестовые пользователи

**Владелец:**
- Email: `bocan.anton@mail.ru`
- Пароль: `password123`

**Оператор:**
- Email: `bocan.anton1@mail.ru`
- Пароль: `password123`

## WebSocket (Real-time обновления)

Клиент автоматически подключается к WebSocket серверу при входе:

```typescript
// Подключение происходит в AuthContext
socketService.connect(companyId);

// События:
// - container_updated - обновление контейнера
// - location_updated - обновление площадки
```

### Как это работает:

1. При входе клиент подключается к WebSocket
2. Присоединяется к комнате своей компании
3. При изменении данных на сервере (симулятор или реальные датчики)
4. Сервер отправляет событие всем клиентам в комнате
5. UI автоматически обновляется без перезагрузки страницы

## Переменные окружения

| Переменная | Описание | Пример |
|------------|----------|--------|
| `VITE_API_URL` | URL API сервера | `https://eco-tracker-server.onrender.com/api` |

## Разработка

### Добавление новой страницы

1. Создайте компонент в `src/pages/`
2. Добавьте роут в `src/pages/Index.tsx`
3. Добавьте ссылку в `src/components/Layout.tsx`

### Добавление нового API метода

1. Добавьте функцию в `src/services/dataService.ts`
2. Используйте `apiClient` из `src/services/api.ts`

### Стилизация

Проект использует **Tailwind CSS**:
```tsx
<div className="flex items-center justify-center p-4 bg-blue-500">
  Content
</div>
```

## Сборка и деплой

### Локальная сборка

```bash
npm run build
npm run preview  # Предварительный просмотр
```

### Деплой на Render

См. `RENDER_DEPLOYMENT.md` для инструкций по развертыванию на Render.

## Troubleshooting

### Проблема: CORS ошибка
**Решение:** Убедитесь, что сервер настроил CORS для вашего домена в `CORS_ORIGINS`

### Проблема: WebSocket не подключается
**Решение:** 
- Проверьте URL в `.env.local`
- Убедитесь, что сервер запущен
- Проверьте консоль браузера на ошибки

### Проблема: 401 Unauthorized
**Решение:** 
- Токен истек - выйдите и войдите снова
- Или очистите localStorage: `localStorage.clear()`

### Проблема: Данные не обновляются
**Решение:**
- Проверьте подключение WebSocket в консоли
- Должно быть: `✅ WebSocket connected`
- Выйдите и войдите снова

## Лицензия

MIT

## Контакты

Разработчик: Anton Bocan  
Email: bocan.anton@mail.ru

